/**
 * Placeholder types. Once your Supabase project is created and the
 * migration in supabase/migrations/0001_init.sql is applied, generate
 * the real types with:
 *
 *   npx supabase gen types typescript --project-id <your-project-id> --schema public > types/database.ts
 *
 * Until then this loose shape keeps the app compiling.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
